interface:
  display_name: "Partner Report Sync"
  short_description: "Connect and schedule daily session collection"
  default_prompt: "Use $partner-report-sync to connect Partner Report or run daily-collect and return the safe collection summary."
