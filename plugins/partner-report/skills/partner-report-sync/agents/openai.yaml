interface:
  display_name: "Partner Report Sync"
  short_description: "Collect complete sessions every Friday"
  default_prompt: "Use $partner-report-sync to run weekly-collect and return the safe collection summary."
